(() => {
    /********** Strings ************/
    let text: string;
    text = "This is a text 'string' ";

    console.log(text);

    const summary = `
        title: ${text}
        price: 500$
    `;

    console.log(summary);
})();